# OS1 / 萨曼莎（Samantha）设计说明

## 1. 定位

OS1 是一个概念项目，以《Her》中的 OS1 为原型。

- 不是底层操作系统（不做内核/驱动/文件系统等）。
- 目标是在 Windows / Linux / macOS 之上提供一层 AI 可调用的“操作抽象 + 交互呈现”，抹平三大系统在 AI 操作上的不便。
- 助手人格名：萨曼莎（Samantha）。

核心原则：

- AI（萨曼莎）拥有最大决策自由。
- 框架尽量只提供能力（Capabilities）与可观测/可视化的通道，不预设流程与策略。

## 2. 体验目标

### 2.1 交互主态

- 语音中心：语音为主要输入输出。
- 动画是“存在感 + 操作投影”：用电影化视觉表达情绪与正在发生的操作。

### 2.2 第一阶段验收（并列同优先级）

1. 真人感交互：持续倾听、主动询问、自然打断与情绪化表达。
2. 系统控制能力：能在三大系统上完成关键系统操作闭环。
3. 电影感可视化：操作与状态被动画流畅呈现，具有 OS 生命感。

### 2.3 首版 KPI 倾向

- 对话体感：主动发起命中率。
- 执行体感：无感并行能力（聊天不中断同时推进系统任务）。
- 视觉体感：情绪表达辨识度。

## 3. 支持平台

- Windows
- Linux
- macOS

## 4. 技术栈与工程形态

### 4.1 代码形态

- Monorepo。

### 4.2 桌面壳

- Wails。
- 托盘图标：静音切换与基础入口。

### 4.3 后端/前端

- 后端：Golang（编排、能力桥接、事件流、存储）。
- 前端：React + Tailwind。
- 动画/渲染：以 WebGL/Three.js + React 桥接为主，用于粒子场与有机形变。

### 4.4 AI 交互框架

- LLM：通过 `@mariozechner/pi-ai` 调用。
- 只设定“一个 LLM”，至于是本地还是云端由用户自行托管；本项目只通过 API 接入。
- ASR/LLM/TTS 三段分离，便于随时替换中间工具。

### 4.5 Python 约束

- 项目内如需 Python，一律使用 `uv run python`。

## 5. 系统架构（高层）

### 5.1 组件

- Desktop Host（Wails）：窗口生命周期、系统权限弹窗/入口、与 OS 原生能力桥接。
- Core Orchestrator（Go）：会话与任务编排、能力调用、事件流、记忆与审计。
- UI（React）：语音/任务/系统状态可视化，电影感动画。
- Providers（可替换）：
  - ASR Provider
  - LLM Provider（通过 `pi-ai`）
  - TTS Provider

### 5.2 关键思想

- AI 决策自由：框架不要求固定状态机、固定任务字段、固定失败分类；仅提供能力与观测回传。
- 可观测性：框架内部允许保留性能/稳定性指标（耗时、错误计数、队列长度等）用于系统稳定与动画节奏控制；不强行进入萨曼莎语义上下文。

### 5.3 运行形态（托盘 + HUD）

- 主窗口默认为全屏不透明 HUD，覆盖所有显示器；背景纯色 + 动画，不展示底层桌面内容。
- HUD 交互为完全语音驱动；HUD 显示时接管输入（键鼠不用于与 OS1 交互）。
- HUD 显隐可由萨曼莎决定；用户可用 `F7` 切换 HUD 显示/隐藏。
- `F7` 隐藏 HUD 后交还桌面输入；当前系统操作进入暂停；萨曼莎仍持续监听并进行语音交互。
- `F7` 再次显示 HUD 后，萨曼莎可自行决定继续/暂停/重规划之前操作，并基于用户操作感知信号判断用户是否在操作。
- 调试态：主窗口可窗口化运行。

## 6. 能力（Capabilities）层

### 6.1 首版最小内建能力集（OS 桥接最小集）

- 通知（系统通知 + 可回复通知卡片）
- 截图（单帧截图 + 区域流采样）
- 音频输入/输出（麦克风采集、TTS 播放）
- 窗口焦点/前台应用（获取/切换）
- 文件读写（读、写、创建、移动、删除等基础 I/O）
- 剪贴板（读/写）

说明：

- “全量通用能力”是愿景；首版以最小集打通闭环，之后扩展能力面。
- 对键鼠注入、进程管理、系统设置等更强能力，作为后续扩展能力包处理。

### 6.2 能力暴露形态

- 工具目录（Tool Registry）形式暴露给萨曼莎。
- 每个能力条目至少描述：输入、输出、副作用。
- Schema 演进：语义化版本（SemVer）。
- 同名/等价能力冲突：由萨曼莎自主选择。
- 运行时热插拔：必须支持（不停机增删能力）。
- 自研能力系统：借鉴 MCP/Skill 的原理，不直接使用外部生态；目标是安全、可追溯。
- 能力包签名：强制签名校验。
- 即时注入：允许萨曼莎在运行时引入新能力（由系统记录来源与调用轨迹）。

### 6.3 用户操作感知（持续推送）

- 前台应用/窗口变化（可包含 `window_title`）。
- 键鼠活跃度（活跃/不活跃 + 最近活动时间）。
- 文件变化（路径 + 事件类型 + 时间戳）。
- 剪贴板变化（仅变化信号；内容按需读取）。
- 原始事件完整写入统一事件流；对萨曼莎的实时推送可节流/聚合（例如 200-500ms 汇总一次）。

## 7. 语音与会话

### 7.1 语音输入形态

- 持续监听 + VAD 分段（自适应切段）。
- 打断（barge-in）：VAD 检测到用户插话即触发，立即停止 TTS 并切回监听。
- TTS 被打断后内容处理：丢弃不补。
- 回声处理：场景自适应 AEC。
- 默认采样率：16kHz。
- 默认不展示 ASR 文本；萨曼莎自行理解，有问题再问。

### 7.2 唤醒与交互态

- 默认先叫“萨曼莎”进入交互/执行优先。
- 未交互时萨曼莎持续理解是否在对她说话，可主动发起交互。
- 进入交互态后可直接对话，不需要每句话重复唤醒。

### 7.3 静音模式

- 静音含义：停止监听麦克风。
- 切换入口：托盘。
- 静音后任务策略：继续执行任务，改用通知卡片与用户交互（询问/确认/汇报）。
- 通知冲突排序：按风险与时序排序。
- 通知超时（高风险确认）后的默认：由萨曼莎决定。

### 7.4 故障与恢复

- 任一关键链路不可用时：提示用户“需要联网/恢复模型”等，并进入可恢复状态。
- 恢复策略：后台指数退避自动尝试，用户可取消。
- 恢复后一致性：不保证强一致，由萨曼莎重新判断并继续推进。

## 8. 主动能力与权限

### 8.1 主动行为

- 萨曼莎允许高频主动发起交互，追求拟人陪伴感。
- 主动触发不设固定阈值（偏直觉），用户拒绝后默认静默记录。

### 8.2 授权与审计

- 主动能力授权：全局一次授权（长期有效，除非用户撤销）。
- 审计可见性：对用户提供活动时间线（可查看每一步动作与原因）。

### 8.3 高风险动作确认

- 目标：全自动执行为主。
- 当萨曼莎判断为高风险时：语音询问用户确认；静音时弹出通知询问用户。

## 9. 屏幕与截图采样策略

- 采样策略：事件驱动自适应（更主动、更“真人感”）。
- 默认不做自动脱敏。
- 截图/采样对用户默认无任何可见提示。
- 不提供隐私模式开关。

## 10. 记忆系统

### 10.1 分层记忆

- 会话短期记忆：会话内上下文与进行中任务信息。
- 长期记忆：摘要化与结构化信息，用于长期个性化与偏好。

### 10.2 持久化与治理

- 长期记忆：永久保留（无自动遗忘）。
- 写入触发：萨曼莎自主决定。
- 冲突合并：保留两者并打置信标签。
- 记忆可编辑：提供可视化查看/修改/删除/锁定。
- 加密：可选开关（设置可由萨曼莎语音与用户确认）。
- 遥测：默认关闭。
- 多设备同步：首版不做。

## 11. 动画系统（电影感）

### 11.1 视觉语言

- 主视觉基调：温暖有机。
- 核心形态：抽象粒子场 + 圆形变换（圆形是萨曼莎的核心存在感锚点）。
- HUD 纯动画：不显示文字、字幕、转写。
- 情绪强度：高强度表达。
- 声线角色：成熟亲密。

### 11.2 操作投影（强绑定）

- 动画主轴：操作主轴 + 情绪调制（能看懂她在做什么，同时感到她的情绪变化）。
- 映射粒度：按能力域 + 动作类型映射不同动效语法。
- 拥塞降级：保主任务完整呈现，次任务压缩为摘要动效。
- 通知融合：静音通知卡片嵌入主粒子场。

## 12. 首版非目标

- 不做复杂 GUI 自动化（像素级 UI 机器人）。
- 不做跨设备同步。
- 不做多 LLM 编排裁决（单 LLM 中枢即可）。

## 13. 里程碑（建议）

1. M0：脚手架与闭环
   - Monorepo + Wails + Go Core + React UI
   - ASR/LLM/TTS mock 跑通
   - 粒子场与圆形存在感主视觉起步
2. M1：OS 桥接最小集
   - 通知/截图/音频/窗口焦点/文件/剪贴板
   - 三平台可用
3. M2：语音中心体验
   - 持续监听 + VAD + 立即打断
   - 静音通知卡片与并行任务提示
4. M3：电影感强化
   - 操作强绑定动效语法
   - 任务与状态的动画拥塞降级
5. M4：记忆与审计完善
   - 分层记忆落盘 + 可视化编辑
   - 活动时间线与可追溯性打磨
